# Implementation Plan: Dynamic Bilingual Receipt Product Name Translation

This plan details the implementation of a dynamic, dictionary-based product name translator for print receipts. 

## User Review Required

- **Full Hindi Translation Preference**: Brand names (e.g. `Ashirvaad` -> `आशीर्वाद`, `Tata` -> `टाटा`) and grocery items (e.g. `Atta` -> `आटा`, `Salt` -> `नमक`) will be translated to Hindi script.
- **Unit and Quantity Preservation**: Quantities and units (e.g. `5kg`, `1kg`, `50g`, `1L`) will remain exactly as written in English to preserve packaging context.
- **Bilingual Format**: In `bilingual` mode, product names will print as `Hindi / English` (e.g., `आशीर्वाद आटा 5kg / Ashirvaad Atta 5kg`) to match other receipt elements.

## Proposed Changes

### Frontend Component: Receipt Translation Helper

#### [MODIFY] [receiptTranslations.ts](file:///c:/MANAS/Projects/LK/Implementation/frontend/src/features/billing/receiptTranslations.ts)
* Create `translateProductName(name: string, lang: Language): string` function.
* Define a translation dictionary mapping common Kirana brands (`amul`, `tata`, `fortune`, `ashirvaad`, etc.), product types (`atta`, `salt`, `sugar`, `oil`, etc.), and qualifiers (`gold`, `classic`, `premium`, etc.).
* Split name into tokens, match multi-word phrases first, map single words next, and keep quantities/units intact.

---

### Frontend UI: Receipt Previewers

#### [MODIFY] [ReceiptPreview.tsx](file:///c:/MANAS/Projects/LK/Implementation/frontend/src/shared/ui/ReceiptPreview.tsx)
* Import `translateProductName` from `receiptTranslations.ts`.
* Render product names using `translateProductName(item.product_name, lang)`.

#### [MODIFY] [ReceiptTab.tsx](file:///c:/MANAS/Projects/LK/Implementation/frontend/src/features/settings/ReceiptTab.tsx)
* Import `translateProductName` from `receiptTranslations.ts`.
* Render product names using `translateProductName(item.product_name, lang)` in the live invoice preview.

## Verification Plan

### Manual Verification
1. Open receipt layout settings, switch the language to **Hindi Only**, and verify that preview items translate to Hindi (e.g. `टाटा नमक 1kg`, `पारले-जी गोल्ड बिस्कुट`).
2. Switch to **Bilingual**, and verify items render as `Hindi / English` (e.g. `टाटा नमक 1kg / Tata Salt 1kg`).
3. Switch back to **English Only**, and verify they render in original English.
