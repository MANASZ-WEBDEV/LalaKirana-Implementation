export default function EODEntryPage() {
  return (
    <div>
      <p style={{ color: 'var(--color-on-surface-variant)' }}>End-of-day sales entry diary and cash totals will load here.</p>
    </div>
  );
}
