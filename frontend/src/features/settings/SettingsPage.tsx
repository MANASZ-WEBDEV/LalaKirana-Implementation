export default function SettingsPage() {
  return (
    <div>
      <p style={{ color: 'var(--color-on-surface-variant)' }}>Staff, login sessions, category configuration, and account password changes will load here.</p>
    </div>
  );
}
