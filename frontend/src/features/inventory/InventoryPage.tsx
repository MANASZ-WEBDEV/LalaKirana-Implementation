export default function InventoryPage() {
  return (
    <div>
      <p style={{ color: 'var(--color-on-surface-variant)' }}>Product catalog, categories, search, and action menus will load here.</p>
    </div>
  );
}
