export default function BulkPricePage() {
  return (
    <div>
      <p style={{ color: 'var(--color-on-surface-variant)' }}>Morning batch price updates and atomic price history saves will load here.</p>
    </div>
  );
}
